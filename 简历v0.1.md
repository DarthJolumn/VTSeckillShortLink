# LiveMall 简历 v0.1

> 技术栈：JDK 25（虚拟线程）、Spring Boot 4.1、Redis Cluster（Lua）、Kafka、MySQL、gRPC、Dubbo、Caffeine、Sentinel、ZGC

---

## 🔥 秒杀抢购

**多级拦截（漏斗模型）**：Gateway HMAC-SHA256 签名 + Nonce 防重放 + JWT 鉴权 + Sentinel 限流 → BloomFilter（1% FPP 过滤无效活动）→ Caffeine soldOutCache（1s TTL 本地拦截售罄）→ Redis Cluster Lua 原子 DECR → Kafka 异步下单

**库存原子性 + Fail Fast**：Redis Lua 实现"查重-扣减-标记"原子操作（Hash Tag 解决集群槽路由），杜绝超卖；Redis 扣减成功后发 Kafka 消息（3s 超时），Kafka 不可用时立即同步回补 Redis 库存，杜绝"库存已扣但订单未建"的资损风险。

**全链路分层幂等，任一环节重试不超卖**：
- Redis `ordered:{aid}:{uid}`（24h TTL）防同一用户重复下单
- Kafka 手动 ack + 异常不确认触发自动重投，保障零丢单
- MySQL `uk_activity_user` 唯一索引兜底，捕获 DuplicateKeyException 后幂等 ack
- Lua EXISTS 前置检查避免退款重复回补
- `@Version` 乐观锁防并发更新冲突

**Consumer 限流与容错**：Semaphore(N) 严格对齐 HikariCP 池大小（预留连接给健康检查与非秒杀查询）；捕获 TransactionException（含 CannotCreateTransactionException）时拒绝 ack 触发重投。

**超时兜底**：TimeoutCancelScheduler 定时扫描待支付订单，乐观锁 CAS 更新状态 + Lua 回补库存，解决"占库不付"的库存悬挂问题。

**双 RPC 通道**：gRPC（Spring Boot 4.1 自动配置 + HTTP/2 多路复用）实时推送秒杀结果，解耦前端轮询；Dubbo 负责内部服务治理（SPI / 负载均衡 / Nacos 注册发现）。

**性能调优与容量治理**：
**JVM 低延迟保障**：秒杀对 P99 稳定性敏感，G1 在大堆高分配下 STW 随存活对象线性增长；同口径压测（450 并发/扣减10w库存/90s），G1 GC耗时 856ms（均为STW Evacuation Pause），分代ZGC 耗时 3ms（均为Proactive主动触发），彻底排除 GC 抖动对延迟的干扰。
**瓶颈定位**：SkyWalking 链路追踪定位 Kafka Producer 同步发送等待，最长 3342ms 为瓶颈。异步削峰后全链路阶梯加压（10-600并发）确认：300 并发 TPS 峰值 1513 / P99 92ms 为吞吐最优平衡点，400 并发反升至 1661 / P99 122ms，600 线程 10s 快斜坡可达 2065 TPS。单机安全限流阈值 300 并发 / 1500 QPS。
**IO 源头背压与可靠投递**：无并发控制时消息涌入抢占 HikariCP 导致事务超时，61.3% 丢单率。Kafka 消费端在载体线程层前置 Semaphore 背压，信号量耗尽时阻塞 poll 循环迫使 Broker 降速，根除 VT 堆积引发的连接耗尽与 OOM；配合手动 ACK + 幂等去重 + 内存分级重试，压测验证 100% 入库、消息零丢失。

---

## 💬 弹幕互动

...

---

## 🏆 实时排行榜

...

---

## 附录：秒杀模块面试速查表

| 追问 | 应答核心 |
|---|---|
| 为什么选 ZGC 不选 G1？ | 秒杀对 P99 敏感，G1 Mixed GC 停顿可达数十毫秒；ZGC 读屏障 + 染色指针 + 并发转移，STW 与堆解耦。同压测（450/45s）验证：G1 GC 总耗时 0.856s（纯 STW），ZGC 仅 0.002s（纯并发）。 |
| STW 这么低为什么 TPS 还会降？ | ZGC 排除 JVM 干扰后，SkyWalking 定位 Kafka Producer 同步发送等待 3342ms 为瓶颈。异步化后 P99 降 65%。300→400 线程 TPS 1513→1661 增长减缓，超 400 后资源饱和，锁竞争反超收益。G1 的 GC 和 I/O 瓶颈叠加会混淆归因。 |
| ZGC 有什么缺点？ | 读屏障约 3-5% 吞吐损耗，内存占用略高。压测确认损耗可接受（TPS 1513 已达标），配合 JFR 持续监控分配速率防退化。 |
| Kafka 不可用且回补 Redis 也失败怎么办？ | 本地内存队列 + 定时重试作三级兜底；仍失败记日志 + 告警，对账任务异步修复。绝不阻塞主流程。 |
| Semaphore 为什么不等于 HikariCP 最大值？ | 许可数 = maxPoolSize - 预留连接数。预留给健康检查和非秒杀查询，避免秒杀占满连接池导致其他功能瘫痪。 |
| Caffeine 1s TTL 会不会导致超卖？ | 不会。L1 仅拦截"已售罄"状态，真正扣减在 Redis Lua 原子执行。1s 窗口内即使 L1 未更新，Redis 层仍能正确拒绝超额请求。 |
| 为什么 Hash Tag？ | Redis Cluster 按 key 哈希槽路由，Lua 脚本内多 key 操作必须同槽。`{activityId}` 保证同一活动的库存 key / 下单标记 key 落在同一节点。 |
| 虚拟线程这么好为什么还要加 Semaphore？ | VT 创建成本低但下游连接池有上限。无限制创建 VT 会导致连接池耗尽 + 调度开销反超 IO 收益。Semaphore 将并发 VT 控制在连接池水位内。 |
| synchronized Pinning 是什么？怎么解决？ | VT 在 synchronized 块内执行阻塞 IO 会钉住载体线程，退化为平台线程。排查 Redisson 版本后升级 + 替换为 ReentrantLock + JFR 监控 VirtualThreadPinned 事件。 |

**口述一致性提醒**：说"Fail Fast"不说"快速失败"，说"幂等 ack"不说"去重确认"，说"染色指针"不说"颜色标记"——与简历用词保持一致。

**证据备查**：`docs/A实测/压测结果/压测数据汇总-G1vsZGC.md` 完整数据表 + `ZGC300-450.csv` 原始 CSV + SkyWalking 截图随身备查。
