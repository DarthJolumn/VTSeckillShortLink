<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <groupId>com.jolumn</groupId>
    <artifactId>vt-seckill-shortlink</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>vt-seckill-shortlink</name>
    <description>VTSeckillShortLink — 高性能秒杀与短链接系统父工程</description>

    <properties>
        <java.version>25</java.version>

        <!-- ====== 核心框架 ====== -->
        <spring-boot.version>4.1.0</spring-boot.version>
        <spring-cloud.version>2025.1.2</spring-cloud.version>
        <spring-cloud-alibaba.version>2025.1.0.0</spring-cloud-alibaba.version>

        <!-- ====== RPC ====== -->
        <dubbo.version>3.3.6</dubbo.version>
        <sentinel.version>1.8.9</sentinel.version>

        <!-- ====== 消息 ====== -->
        <!-- 由 spring-boot-dependencies BOM 管理 → kafka-clients 4.2.1 -->

        <!-- ====== 缓存 ====== -->
        <caffeine.version>3.2.0</caffeine.version>

        <!-- ====== 密码 ====== -->
        <jbcrypt.version>0.4</jbcrypt.version>

        <!-- ====== 工具 ====== -->
        <hutool.version>5.8.28</hutool.version>

        <!-- ====== 可观测 ====== -->
        <micrometer-tracing.version>1.2.5</micrometer-tracing.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Cloud BOM -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Spring Cloud Alibaba BOM (Nacos + Sentinel) -->
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-alibaba-dependencies</artifactId>
                <version>${spring-cloud-alibaba.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- SCA 2025.1.0.0 自动配置 NacosDataSource 但没带 sentinel-datasource-nacos -->
            <dependency>
                <groupId>com.alibaba.csp</groupId>
                <artifactId>sentinel-datasource-nacos</artifactId>
                <version>${sentinel.version}</version>
            </dependency>

            <!-- Dubbo -->
            <dependency>
                <groupId>org.apache.dubbo</groupId>
                <artifactId>dubbo-spring-boot-starter</artifactId>
                <version>${dubbo.version}</version>
            </dependency>

            <!-- Caffeine -->
            <dependency>
                <groupId>com.github.ben-manes.caffeine</groupId>
                <artifactId>caffeine</artifactId>
                <version>${caffeine.version}</version>
            </dependency>

            <!-- BCrypt -->
            <dependency>
                <groupId>org.mindrot</groupId>
                <artifactId>jbcrypt</artifactId>
                <version>${jbcrypt.version}</version>
            </dependency>

            <!-- Hutool -->
            <dependency>
                <groupId>cn.hutool</groupId>
                <artifactId>hutool-all</artifactId>
                <version>${hutool.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- ====== 子模块 ====== -->
    <modules>
        <module>vtsl-common</module>
        <module>vtsl-user</module>
        <module>vtsl-seckill</module>
        <module>vtsl-product</module>
        <module>vtsl-shortlink</module>
        <module>vtsl-websocket</module>
        <module>vtsl-leaderboard</module>
        <module>vtsl-gateway</module>
    </modules>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <compilerArgs>
                            <arg>-proc:full</arg>
                        </compilerArgs>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <skip>false</skip>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <!-- spring-boot-maven-plugin 配置在 pluginManagement 中，子模块按需继承 -->
        </plugins>
    </build>

</project>
