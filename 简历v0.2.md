# LiveMall 简历 v0.2

> 技术栈：JDK 25（虚拟线程）、Spring Boot 4.1、Redis Cluster（Lua）、Kafka、MySQL、gRPC、Dubbo、Caffeine、Sentinel、ZGC

---

## 🔥 秒杀抢购

（覆盖秒杀 16 大架构中的：业务架构、异步架构、分层架构、缓存架构、数据一致性、MQ 异步、降级架构、存储高可用、运维部署，共 9 项）

**多级拦截漏斗模型**：Gateway HMAC 签名 + Nonce 防重放 + JWT 鉴权 + Sentinel 限流 → Caffeine 活动信息精确判存 → Redis Cluster Lua 原子扣减 → Kafka 异步下单 → WebSocket 推送结果

**数据一致性五道防线**：Lua 脚本内"查重-扣减-标记"原子完成，Hash Tag `{activityId}` 保证 Cluster 同槽无 CROSSSLOT；DB 唯一索引 `uk_activity_user` + DuplicateKeyException 幂等 ack；扣减强制走 Master 禁读写分离，杜绝主从异步复制超卖；Redis 返回 null 时抛 503 而非误导库存不足；退款 Lua EXISTS 前置校验保证幂等回补。

**分布式事务与最终一致性**：取消订单 DB 先（`@Version` CAS 防并发）→ Redis 后（Lua EXISTS 幂等回补），ReconciliationScheduler 每 5 分钟对账兜底 Redis refund 失败，杜绝库存泄漏。

**高并发消费与稳定性保障**：Kafka 消费端 Semaphore 背压（上限对齐 HikariCP 池大小 30），信号量耗尽主动阻塞 poll 循环迫使 Broker 降速，从根源杜绝虚拟线程堆积引发的连接耗尽与 heap OOM。配合手动 ACK + 幂等去重 + 内存分级重试（TransactionException 重试 3 次），压测验证 100% 入库、消息零丢失。

**性能调优与容量治理**：
**JVM 延迟治理**：同口径压测（450 并发/扣减 10w 库存/90s/堆峰值 4GB）下 G1 因同步 Evacuation 产生 856ms STW；切换分代 ZGC 后 GC 耗时降至 3ms（均为 Proactive 主动触发），彻底排除 GC 抖动对延迟的干扰。
**瓶颈定位与容量规划**：阶梯加压（10 → 450 并发 / 90s 稳态），定位 MySQL 写入为瓶颈：300 并发 TPS 峰值 1408 / P99 264ms，450 并发反降至 1298 / P99 309ms，据此推断单机安全限流阈值 300 并发 / 1400 QPS。

**双 RPC 通道**：gRPC（HTTP/2 多路复用）实时推送秒杀结果，后改为 Redis Pub/Sub 多实例广播解决单点推送问题；Dubbo 负责内部服务治理（SPI / 负载均衡 / Nacos 注册发现）。

**存储与部署高可用**：Redis Cluster 3 主 3 从 + `read-from: MASTER`；HikariCP 连接池保护 MySQL；Docker 容器化部署 + Nacos 服务注册发现；优雅停机 `@PreDestroy` 关闭 gRPC channel。

---

## 💬 弹幕互动

...

---

## 🏆 实时排行榜

...

---

## 附录：秒杀模块面试速查表

| 追问 | 应答核心 |
|---|---|
| 为什么选 ZGC 不选 G1？ | 秒杀对 P99 敏感，G1 Mixed GC 停顿可达数十毫秒；ZGC 读屏障 + 染色指针 + 并发转移，STW 与堆解耦。同压测（450/45s）验证：G1 GC 总耗时 0.856s（纯 STW），ZGC 仅 0.002s（纯并发）。 |
| STW 这么低为什么 TPS 还会降？ | 这正是压测价值——ZGC 排除 JVM 干扰后才敢确认瓶颈在 MySQL（~1400 TPS）。450 并发时请求排队等 DB I/O，锁竞争开销反超并发收益。G1 的 0.856s GC 和 MySQL 瓶颈叠加会混淆归因。 |
| ZGC 有什么缺点？ | 读屏障约 3-5% 吞吐损耗，内存占用略高。压测确认损耗可接受（TPS 1408 已达标），配合 JFR 持续监控分配速率防退化。 |
| 分布式事务怎么处理的？ | 取消订单：DB 先（@Version CAS 防并发）、Redis 后（Lua EXISTS 幂等回补）。如果 Redis 回补失败，ReconciliationScheduler 每 5 分钟扫描 status=2 的订单，检查 ordered key 是否还在 Redis，在则补偿回补。 |
| 如何防止虚拟线程堆积？ | Semaphore(30) 上限对齐 HikariCP 池大小。信号量耗尽 → 阻塞 listener 线程 → poll 暂停 → Broker 感知消费慢 → 降速。从根源杜绝 VT 堆积。 |
| Kafka 消费失败怎么办？ | 手动 ACK：消费成功才 ack。TransactionException 内存重试 3 次，超过后 ack 放弃（幂等兜底，DuplicateKey 不会重复入库）。 |
| 限流用的什么算法？ | 令牌桶（Token Bucket）：固定速率放令牌，请求取令牌，桶空则拒绝。允许突发流量（攒令牌后短时间放行一批），适合秒杀场景。漏桶（Leaky Bucket）强制匀速输出，不适合突发。 |
| 为什么不直接用 Redis BF（布隆过滤器）？ | 活动量级小（几十到几百个），Caffeine 精确判存就够了。BF 在多实例间不一致（各自独立维护），反而会导致误杀 404。Caffeine 精确判存零误判、零同步开销。 |
| soldOutCache 为什么删了？ | 1s TTL 的本地售罄缓存，其他实例 1s 内不知道已售罄 → 多笔无效 Redis deduct。活动量级小，删除后直接走 Redis Lua deduct 返回 -2 判断售罄，零延迟、零不一致。 |
| 为什么 Hash Tag？ | Redis Cluster 按 key 哈希槽路由，Lua 脚本内多 key 操作必须同槽。`{activityId}` 保证同一活动的库存 key / 下单标记 key 落在同一节点。 |
| 虚拟线程这么好为什么还要加 Semaphore？ | VT 创建成本低但下游连接池有上限。无限制创建 VT 会导致连接池耗尽 + 调度开销反超 IO 收益。Semaphore 将并发 VT 控制在连接池水位内。 |

**口述一致性提醒**：说"Fail Fast"不说"快速失败"，说"幂等 ack"不说"去重确认"，说"染色指针"不说"颜色标记"——与简历用词保持一致。

**证据备查**：`docs/A实测/压测结果/压测数据汇总-G1vsZGC.md` 完整数据表 + `ZGC300-450.csv` 原始 CSV + SkyWalking 截图随身备查。
