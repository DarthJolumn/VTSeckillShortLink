# 匿名观看设计 v1/v2 — 冲突与待办清单

> 2026-07-15 对 `6.2-匿名观看与按需登录设计-v1.md`、`-v2.md` 与现有全部设计文档做交叉对比的结果。
> 按优先级排序，标注责任方（改新文档 / 改现有文档 / 两者都改）。

---

## CRITICAL（不改系统跑不起来）

| # | 冲突 | v1/v2 说 | 现有文档说 | 责任方 | 备注 |
|---|---|---|---|---|---|
| C1 | WS 无效 Token 行为 | 降级匿名，不断连 | close(4001) 断连 | **现有**（2.4, WebSocket模块实现设计） | 核心：匿名模型要求 |
| C2 | WS token 必传/可选 | `?token=` 可选 | 必传 | **现有**（2.4, 2.8, WebSocket模块实现设计） | |
| C3 | `/auth/refresh` 在白名单 | 在 public-paths | 不在（会 401） | **现有**（2.6, 3.6.2）+ 代码 | 不改则刷新接口不可用 |
| C4 | Gateway 白名单结构 | 二级：public-paths + public-get-paths | 一级扁平 WHITE_LIST | **现有**（2.6, 3.6.2）+ 代码 | |
| C5 | `/ws/**` 在白名单 | 在 public-paths | 不在 | **现有**（2.6, 3.6.2）+ 代码 | 不改 WS 握手被拦 |
| C6 | 缺少 `/live/**` 路由 | Gateway 需路由到 websocket | 不存在此路由 | **现有**（2.6）+ Gateway yml | |
| C7 | 排行榜需登录 | GET 公开 | 标记 JWT 鉴权 | **现有**（2.8） | |
| C8 | JWT userId 取法 | `getSubject()` | `claims.get("userId")` | **现有**（2.6, 3.6.2） | JwtUtil 实际存在 sub 里，现有文档写法错误 |
| C9 | `GET /live/room/{id}` | 新端点 | 不存在 | **现有**（2.8） | |
| C10 | @OnOpen 完整重设计 | 可选 token + 匿名降级 + AUTH 升级 | 必须 JWT + Redis userId 操作 | **现有**（WebSocket模块实现设计, 2.4） | |

---

## HIGH（架构不一致，不改也能跑但逻辑不通）

| # | 冲突 | v1/v2 说 | 现有文档说 | 责任方 | 备注 |
|---|---|---|---|---|---|
| H1 | UserContext 结构 | 三个独立 ScopedValue（USER_ID, ROLE, DEVICE_ID） | 单个 ScopedValue<User>（2.1）或 ScopedValue<Long>（1.4） | **新文档建议为准**，现有文档（2.1, 1.4）需同步 | |
| H2 | UserContext 包名 | `context/` | `util/` | **新文档为准** | |
| H3 | Dubbo 接口包名 | `service/`（v1, v2） | `api/`（CLAUDE.md, 2.1） | **改新文档** → `api/` | 应遵循现有规范 |
| H4 | WsSession.userId | nullable Long | non-null Long（WebSocket模块实现设计） | **现有**（WebSocket模块实现设计） | |
| H5 | 房间成员计数 | 匿名用户无 userId | Redis Set 只存 userId（2.4, WebSocket模块实现设计） | **现有** | 需改用 sessionId 或分离计数器 |
| H6 | 新 WS 消息类型 | AUTH / AUTH_OK / NEED_AUTH | 协议中不存在（2.4, 2.8, WebSocket模块实现设计） | **现有** | |
| H7 | 弹幕走 HTTP vs WS | v1 §6.2 示例用 `POST /live/barrage`（注解演示） | WS 上行 BARRAGE 消息 | **改新文档** → 标注"仅为注解示例，实际走 WS" | |
| H8 | Redis 房间 key 三种命名 | 新文档未指定 | `room:{id}` vs `room:members:{id}` vs `ws:room:{id}:online` | **两者都改** → 统一为一个 | 这是一个预存的现有文档 bug |
| H9 | WsMessageHandler 无匿名过滤 | 匿名只允许 PING/AUTH | 无匿名检查 | **现有**（WebSocket模块实现设计） | |

---

## MEDIUM（补全性质的缺陷）

| # | 问题 | 责任方 | 备注 |
|---|---|---|---|
| M1 | Dubbo 返回值包裸 DTO 还是 `Result<T>`——v1 用裸 DTO，现有 2.1 用 `Result<T>` | **改新文档** → `Result<UserDTO>` | |
| M2 | 新 `annotation/` 包未在 2.1 包结构中 | **现有**（2.1） | |
| M3 | v2 OAuth 列缺失——`t_user` 加 `oauth_provider`/`oauth_open_id` | **现有**（2.7） | v2 P2 |
| M4 | `t_audit_log` 表缺失 | **现有**（2.7） | v2 P2 |
| M5 | 匿名追踪 Redis key 未注册 | **现有**（Redis设计文档） | `anonymous:{fp}`, `anonymous:{fp}:rooms`, `device:{fp}` |
| M6 | 限流 Redis key 未注册 | **现有**（Redis设计文档） | `rl:{method}:{dim}:{key}` |
| M7 | StepUp Redis key 未注册 | **现有**（Redis设计文档） | `step-up:{userId}:{method}` |
| M8 | WS `@QueryParam("token")` 未标注可选 | **现有**（WebSocket模块实现设计） | |
| M9 | WsPushService 包名：新文档和 WebSocket模块实现设计都写 `service/`，但规范是 `api/` | **新文档 + 现有**（WebSocket模块实现设计） | |
| M10 | 新功能缺错误码（需注册 1015+） | **新文档** → 补充 | 匿名禁止、限流、StepUp 等 |
| M11 | 威胁模型缺匿名场景 | **现有**（1.6） | 匿名 WS 洪水、公开 API 爬取 |
| M12 | 公开端点缺 Sentinel 规则 | **新文档或现有**（2.6） | `/live/room/*`, `/ws/**`, `/leaderboard/**` |
| M13 | `/auth/test-token` 在新白名单消失 | **改新文档** → 明确此端点命运 | 删除或保留 |
| M14 | Refresh Token Redis key 正反模式——现有文档自相矛盾 | **现有**（1.4代码示例 vs Redis设计文档） | 预存问题 |
| M15 | 审计日志依赖 Kafka，但项目 Kafka 注释中 | **改新文档** → 加 fallback 方案 | v2 §7.5 |
| M16 | AuthInterceptor Order 未在 v1 显式声明 | **改新文档** → 补充 `Order=0` | v2 §9 已用了 Order=0 |
| M17 | v2 RateLimitInterceptor 用 `ReactiveStringRedisTemplate` 但 MVC 应同步 | **改新文档** → `StringRedisTemplate` | v2 §2.4 |
| M18 | v2 联查现有代码发现 UserService 缺少 `findById()`——与最初分析一致 | **现有代码** | 需在 v1 落地时补 |

---

## 新文档缺失项（应在 v1/v2 中补充）

| # | 缺失 | 相关现有文档 |
|---|---|---|
| MS1 | `/auth/test-token` 未交代 | 原有白名单 |
| MS2 | 公开端点 Sentinel 规则 | 2.6 §限流 |
| MS3 | CORS 对匿名浏览器访问影响 | 2.6 §跨域 |
| MS4 | 新功能错误码注册（例：1015=需登录, 1016=限流, 1017=需二次验证） | 2.1 §错误码, 1.4 §三 |
| MS5 | `BarrageRequest`、`RoomInfoVO` 等新 DTO 定义 | — |
| MS6 | 弹窗注册幂等性（`X-Idempotency-Key`） | 2.2 §注册 |
| MS7 | 匿名数据过期清理策略（Redis TTL 7d 后怎么处理） | — |
| MS8 | 匿名请求日志脱敏（IP 记录） | 1.6 §4.4 |
| MS9 | 匿名用户离线消息（明确不做，说明理由） | 2.4 §断线重连 |
| MS10 | Kafka 不可用时审计日志降级方案 | CLAUDE.md（Kafka 注释中） |

---

## 处理策略

```
第一轮（v1 落地前必须修）：
  改现有文档+代码：C1~C10, H4~H6, H8, H9
  改新文档：H3（包名）, H7（弹幕示例）, M1（返回值）, M10（错误码）,
            M13（test-token）, M16（Order）, M17（Redis模板）, M15（Kafka降级）

第二轮（v1 落地后可补）：
  改现有文档：H1, H2, M2~M8, M11, M14, M18
  改新文档：MS1~MS10 全部补充

第三轮（v2 实施前修）：
  改现有文档：M3, M4, M5, M6, M7, M12
```

---

## 关联文档

- v1 设计：`6.2-匿名观看与按需登录设计-v1.md`
- v2 设计：`6.2-匿名观看与按需登录设计-v2.md`
- 安全设计：`1.6-安全设计总览.md`
- Gateway：`2.6-网关与可观测性.md`
- WebSocket：`2.4-WebSocket长连接.md`、`WebSocket模块实现设计.md`
- 公共模块：`2.1-公共模块.md`
- 数据库：`2.7-数据库设计.md`
- Redis：`Redis设计文档.md`
